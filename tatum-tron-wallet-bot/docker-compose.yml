services:
  mongo:
    image: mongo:8
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    depends_on:
      - mongo

volumes:
  mongo_data:
